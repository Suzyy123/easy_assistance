name: easy_assistance_app
description: "A new Flutter project."
publish_to: 'none'

# Application version
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Adjusted for compatibility with Dart 3.x

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4 # Updated to a compatible, working version
  cloud_firestore: ^5.5.1
  shared_preferences: ^2.0.18
  provider: ^6.1.2 # Added a specific compatible version
  http: ^1.2.2
  intl: ^0.20.1 # Added the `intl` package for internationalization and date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/logo.png
    - lib/images/registerPage.png
    - lib/images/google.jpeg
    - lib/images/avatar1.png
    - lib/images/avatar2.png
    - lib/images/avatar3.png
    - lib/images/avatar4.png
    - lib/images/avatar5.png
    - lib/images/avatar6.png
    - lib/images/avatar7.png
    - lib/images/avatar8.png
